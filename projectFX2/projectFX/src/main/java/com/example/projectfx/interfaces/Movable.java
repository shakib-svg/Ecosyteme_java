package com.example.projectfx.interfaces;

public interface Movable {
    /**
     * Moves the entity in a specified direction.
     *
     * @param direction The direction in which the entity moves (e.g., "north", "east").
     */
    void move(String direction);
}